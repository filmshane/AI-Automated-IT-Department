-- Rename to customers_active.sql after DBA review
SELECT c.CustomerId, c.Name, c.Email, c.Phone, c.LastPurchaseDate
FROM dbo.Customers c
WHERE c.IsActive = 1 AND c.Email IS NOT NULL AND c.MarketingOptIn = 1;
